import styles from "./index.module.css";
import { type NextPage } from "next";
import Head from "next/head";
import { useState, useEffect } from "react";

type League = {
  id: number;
  name: string;
}


const Home: NextPage = () => {
  const [leagues ,setLeague] = useState<string | null>()

  useEffect(()=>{
    fetch('api/leagues')
    .then((res)=> res.json())
    .then((data)=> {setLeague(data)
    console.log(leagues,"data from league api")
    })


  },[])
  return (
    <>
      <Head>
        <title>Golf League Beta</title>
        <meta name="description" content="Generated by create-t3-app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <main >
        <h1 className="sm:text-4xl sm:flex sm:justify-center">Correy is gay</h1>
        <div className="sm:flex sm:justify-center">
          <form action="">
            <label htmlFor="">
              League ID
            </label>
            <input type="text" className="border p-2 m-2" />
            <button className="border p-2">Search</button>
          </form>
          <div>
            <ul>

            </ul>
          </div>
        </div>
      </main>
    </>
  );
};

export default Home;
